<UserControl x:Class="AchievementsHelper.View.Controls.AchievementsDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AchievementsHelper.View.Controls"
             Background="Transparent">

    <Border CornerRadius="8"
            BorderBrush="#66C0F4"
            BorderThickness="1"
            Background="#1B2838"
            Padding="4"
            SnapsToDevicePixels="True">

        <DataGrid ItemsSource="{Binding Achievements}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserResizeColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  Background="#1B2838"
                  Foreground="White"
                  FontSize="14"
                  FontFamily="Segoe UI"
                  RowBackground="#2A475E"
                  AlternatingRowBackground="#1B2A38"
                  BorderThickness="0"
                  ColumnHeaderHeight="40"
                  RowHeight="34"
                  HorizontalGridLinesBrush="#3B6A9C"
                  VerticalGridLinesBrush="#3B6A9C"

                  SnapsToDevicePixels="True">

            <DataGrid.Resources>
                <!-- Nagłówki kolumn -->
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2A475E"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="10 6"/>
                    <Setter Property="BorderBrush" Value="#66C0F4"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>

                <!-- Komórki danych -->
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10 6"/>
                    <Setter Property="BorderBrush" Value="#3B6A9C"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>

                <!-- CheckBox w komórce -->
                <Style TargetType="CheckBox">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa"
                                    Binding="{Binding Name}"
                                    Width="*" />
                <DataGridTextColumn Header="Opis"
                                    Binding="{Binding Description}"
                                    Width="2*" />
                <DataGridCheckBoxColumn Header="Odblokowane"
                                        Binding="{Binding Achieved}"
                                        Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Border>
</UserControl>
