<UserControl x:Class="AchievementsHelper.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#1B2838">

    <Border CornerRadius="12" Background="#1B2838" BorderBrush="#66C0F4" BorderThickness="1" Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Nagłówek -->
            <TextBlock Text="Zaloguj się przez Steam"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="#66C0F4"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 20" />

            <!-- WebView2 -->
            <wv2:WebView2 x:Name="webView"
                          Grid.Row="1"
                          Visibility="Collapsed" />

            <!-- Przycisk logowania -->
            <Button x:Name="btnLogin" Grid.Row="2"
                    Content="Zaloguj przez Steam"
                    Command="{Binding LoginCommand}"
                    Width="220"
                    Height="45"
                    Background="#66C0F4"
                    Foreground="Black"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"
                    Margin="0 20 0 0" Click="btnLogin_Click" />
        </Grid>
    </Border>
</UserControl>
